export enum SaleStatusEnum {
   CONFIRMED = 'Confirmed',
   DONE = 'Done',
   CANCELED = 'Canceled',
}

export enum SaleTimeframeEnum {
   DAILY = 'daily',
   WEEKLY = 'weekly',
   MONTHLY = 'monthly',
   ANNUAL = 'annual',
}
